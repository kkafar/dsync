syntax = "proto3";
package services.user_agent;

message HostSpec {
  oneof kind {
    int32 local_host = 1;
    string name = 2;
    int32 local_id = 3;
  }
}

message FileSource {
  HostSpec host_spec = 1;
  oneof path_spec {
    string relative_path = 2;
    string absolute_path = 3;
  };
}
